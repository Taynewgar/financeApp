# Copie para .env.local e preencha (o mesmo Supabase do backend).
# Nunca commite .env.local — já está no .gitignore.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# URL do backend FastAPI. Em dev local, geralmente http://localhost:8000.
# Em produção, a URL do serviço no Render.
VITE_API_BASE_URL=http://localhost:8000
