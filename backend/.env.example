# Copie para .env e preencha com os valores do seu projeto Supabase
# (Project Settings > API, e Project Settings > Database > Connection string).
# Nunca commite o .env — ele já está no .gitignore.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
